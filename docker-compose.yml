---
version: "3.7"

services:
  router:
    restart: always
    build: ./docker
    cap_add:
      - NET_ADMIN
    environment:
      TZ:
      WAN_IF: eth0
      TRUSTED_IF: eth1
    network_mode: host
    tmpfs:
      - /tmp
    volumes:
      - ./tmp:/data
