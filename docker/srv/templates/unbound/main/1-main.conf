include: "/srv/run/unbound/0-base.conf"

server:
  pidfile: /tmp/unbound-main.pid

  interface: {{ LOOPBACK_LOCAL }}
  port: {{ UNBOUND_PORT }}


remote-control:
  control-enable: yes
  control-interface: /tmp/unbound-main.sock

