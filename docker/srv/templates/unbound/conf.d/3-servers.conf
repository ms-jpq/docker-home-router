{% if DNS_ADDRS or DNS_HOSTS %}
forward-zone:
  name: "."
  forward-first: yes

{% for SERVER in DNS_ADDRS %}
  forward-addr: {{ SERVER }}
{% endfor %}


{% for SERVER in DNS_HOSTS %}
  forward-host: {{ SERVER }}
{% endfor %}

{% endif %}
