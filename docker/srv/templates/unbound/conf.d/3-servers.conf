server:

{% if DNSSEC %}
  auto-trust-anchor-file: "/data/unbound/root.key"
{% else %}
  domain-insecure: "."
{% endif %}


{% if DNS_ADDRS %}
forward-zone:
  name: "."
  forward-first: yes

{% for SERVER in DNS_ADDRS %}
  forward-addr: {{ SERVER }}
{% endfor %}

{% endif %}
