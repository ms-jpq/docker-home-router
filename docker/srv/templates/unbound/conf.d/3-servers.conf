{% if DNS_SERVERS OR DNS_TLS_SERVERS %}
forward-zone:
  name: "."

{% for SERVER in DNS_SERVERS %}
  forward-addr: {{ SERVER }}
{% endfor %}


{% for SERVER in DNS_TLS_SERVERS %}
  forward-host: {{ SERVER }}
  forward-tls-upstream: yes
{% endfor %}

{% endif %}
