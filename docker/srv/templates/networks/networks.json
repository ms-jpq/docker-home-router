{
  "guest": {
    "v4": "{{ GUEST_NETWORK_V4 }}",
    "v6": "{{ GUEST_NETWORK_V6 }}"
  },
  "lan": {
    "v4": "{{ LAN_NETWORK_V4 }}",
    "v6": "{{ LAN_NETWORK_V6 }}"
  },
  "tor": {
    "v4": "{{ TOR_NETWORK_V4 }}",
    "v6": "{{ TOR_NETWORK_V6 }}"
  },
  "wireguard": {
    "v4": "{{ WG_NETWORK_V4 }}",
    "v6": "{{ WG_NETWORK_V6 }}"
  }
}
